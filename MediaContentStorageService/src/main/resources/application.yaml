yandex:
  baseUrl: https://cloud-api.yandex.net/v1/disk/resources
  application-path: /chat_application
  oauth-token:  y0__xDP35vDBRjblgMg1_6ogBSr4lVFEiYk8u0pvxDtIO93JeN9-Q

server:
  port: 8088

spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: http://localhost:8085/.well-known/jwks.json

services:
  in-database-image-service: http://localhost:8089

webclient:
  timeout: 10

logging:
  level:
    org:
      springframework:
        web:
          reactive:
            function:
              client:
                ExchangeFunctions: TRACE
