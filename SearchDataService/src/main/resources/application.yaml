server:
  port: 8093

spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: ${AUTH_SERVICE_URL}/.well-known/jwks.json

services:
  auth-service: ${AUTH_SERVICE_URL}
  messages-service: ${MESSAGES_SERVICE_URL}