server:
  port: 8087

spring:
  threads:
    virtual:
      enabled: true

  datasource:
    url: jdbc:postgresql://localhost:5436/messaging_db
    username: alexgls
    password: AS04SERa1

  kafka:
    bootstrap-servers: localhost:29092
    listener:
      auto-startup: false

  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: http://localhost:8085/.well-known/jwks.json

frontend:
  url: http://localhost:8090

services:
  auth-service: http://localhost:8085

values:
  page-size: 10


app:
  security:
    aes-key: ${AES_KEY:0e3b03a3d8f1e8a9c3b0c1d2e3f4a5b6}
    hmac-key: ${HMAC_KEY:c4d5e6f7a8b9c0d1e2f3a4b5c6d7e8f9a0b1c2d3e4f5a6b7c8d9e0f1a2b3c4d5}