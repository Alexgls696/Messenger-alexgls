server:
  port: 8094

storage:
  bucket: alexgls-messenger-media
  access_key: xdarYrmyYcFA6AqfRLT86r #${AWS_ACCESS_KEY_ID}
  secret_access_key: 2t1u2HLgj75PfKg2FiEBKjLXX26E3vxuAWketKkXiytP #${AWS_SECRET_ACCESS_KEY}
  default_region: ru-msk
  endpoint: "https://hb.ru-msk.vkcloud-storage.ru"

spring:
  kafka:
    bootstrap-servers: ${SPRING_KAFKA_BOOTSTRAP_SERVERS:localhost:29092}

  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: ${AUTH_SERVICE_URL:http://localhost:8085}/.well-known/jwks.json

ai-services:
  gigachat:
    auth-url: https://ngw.devices.sberbank.ru:9443/api/v2/oauth
    ai-url: https://gigachat.devices.sberbank.ru/api/v1
    load-files-url: https://gigachat.devices.sberbank.ru/api/v1/files
    auth-token: ${SBER_AUTH_TOKEN}